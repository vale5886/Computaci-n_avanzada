# Models

Esta carpeta queda reservada para una extensión posterior del ejercicio.

Podrás exportar desde Rhino:

```text
model.glb
```

y eventualmente utilizar esa geometría como módulo del sistema.

Para la primera iteración usamos `BoxGeometry` para mantener baja la complejidad técnica.
